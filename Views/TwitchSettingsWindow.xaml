<Window x:Class="OpenBroadcaster.Views.TwitchSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:OpenBroadcaster.ViewModels"
        Title="Twitch Settings"
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource StudioBackgroundBrush}"
        Foreground="{StaticResource StudioMetadataTextBrush}"
        mc:Ignorable="d">

    <Border Background="{StaticResource StudioPanelBrush}"
            BorderBrush="{StaticResource StudioPanelBorderBrush}"
            BorderThickness="1"
            CornerRadius="10"
            Padding="18"
            Margin="12">
        <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Label Content="Username" />
        <TextBox Grid.Row="0"
                 MinWidth="260"
                 Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}" />

        <Label Grid.Row="1" Content="OAuth Token" />
        <PasswordBox x:Name="TokenBox"
                     Grid.Row="1"
                     PasswordChar="•"
                     Loaded="PasswordBox_Loaded" />

        <Label Grid.Row="2" Content="Channel" />
        <TextBox Grid.Row="2"
                 MinWidth="260"
                 Text="{Binding Channel, UpdateSourceTrigger=PropertyChanged}" />

        <Label Grid.Row="3" Content="Points Name" />
        <TextBox Grid.Row="3"
                 MinWidth="260"
                 Text="{Binding PointsName, UpdateSourceTrigger=PropertyChanged}" />

        <Label Grid.Row="4" Content="Request Cost" />
        <TextBox Grid.Row="4"
                 MinWidth="260"
                 Text="{Binding RequestCost, UpdateSourceTrigger=PropertyChanged}" />

        <Label Grid.Row="5" Content="PlayNext Cost" />
        <TextBox Grid.Row="5"
                 MinWidth="260"
                 Text="{Binding PlayNextCost, UpdateSourceTrigger=PropertyChanged}" />

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Width="100" Margin="0,12,8,0" IsCancel="True" />
            <Button Content="Save" Width="120" Margin="0,12,0,0" IsDefault="True" Click="SaveButton_Click" />
        </StackPanel>
        </Grid>
    </Border>
</Window>
