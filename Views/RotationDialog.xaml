<Window x:Class="OpenBroadcaster.Views.RotationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        Title="Edit Rotation"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        MinWidth="480"
        MinHeight="260"
        mc:Ignorable="d">
    <StackPanel Margin="18" Width="440">
        <TextBlock Text="Simple Rotation" FontWeight="Bold" FontSize="16" Margin="0,0,0,12" />

        <Grid Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="90" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Text="Name" VerticalAlignment="Center" />
            <TextBox Grid.Column="1" Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" MinWidth="220" />
            <CheckBox Grid.Column="2" Content="Enabled" IsChecked="{Binding Enabled, Mode=TwoWay}" VerticalAlignment="Center" Margin="0,0,8,0" />
            <CheckBox Grid.Column="3" Content="Active" IsChecked="{Binding IsActive, Mode=TwoWay}" VerticalAlignment="Center" />
        </Grid>

        <TextBlock Text="Category Slots" FontWeight="SemiBold" Margin="0,0,0,6" />
        <ItemsControl ItemsSource="{Binding Slots}" AlternationCount="100">
            <ItemsControl.ItemContainerStyle>
                <Style TargetType="ContentPresenter">
                    <Setter Property="Margin" Value="0,0,0,6" />
                </Style>
            </ItemsControl.ItemContainerStyle>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <DockPanel>
                        <TextBlock Text="Slot" Width="40" VerticalAlignment="Center" />
                        <ComboBox Width="250"
                                  ItemsSource="{Binding DataContext.CategoryOptions, RelativeSource={RelativeSource AncestorType=Window}}"
                                  SelectedItem="{Binding CategoryName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                  Margin="4,0,8,0" />
                        <Button Content="Remove"
                                Width="80"
                                Command="{Binding DataContext.RemoveSlotCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                CommandParameter="{Binding}" />
                    </DockPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <Button Content="Add Slot" Width="120" Margin="0,4,0,8" Command="{Binding AddSlotCommand}" HorizontalAlignment="Left" />

        <TextBlock Text="Pick one or more library categories for AutoDJ to draw tracks from."
                   Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                   Margin="0,4,0,12" />

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="Save" Width="90" Margin="0,0,8,0" IsDefault="True" Click="OnSaveClick" />
            <Button Content="Cancel" Width="90" IsCancel="True" Click="OnCancelClick" />
        </StackPanel>
    </StackPanel>
</Window>
