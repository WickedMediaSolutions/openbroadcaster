<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- Base palette -->
    <Color x:Key="StudioGraphiteBaseColor">#FF02060F</Color>
    <Color x:Key="StudioGraphitePanelColor">#FF061228</Color>
    <Color x:Key="StudioGraphitePanelAltColor">#FF0B1B36</Color>
    <Color x:Key="StudioGraphiteHighlightColor">#FF15345C</Color>
    <Color x:Key="StudioAccentNeonBlueColor">#FF35C8FF</Color>
    <Color x:Key="StudioAccentNeonBlueBrightColor">#FF7CE6FF</Color>
    <Color x:Key="StudioAccentUltravioletColor">#FF8C6CFF</Color>
    <Color x:Key="StudioMutedTextColor">#FF93A9C8</Color>
    <Color x:Key="StudioMetadataTextColor">#FFF4F9FF</Color>
    <Color x:Key="StudioQueueTextColor">#FFE9F1FF</Color>
    <Color x:Key="StudioDangerColor">#FFFF7C6E</Color>
    <Color x:Key="StudioControlBorderColor">#FF1E2C47</Color>
    <Color x:Key="StudioTransportShadowColor">#BF02112A</Color>
    <Color x:Key="StudioLedColor">#FF54FFA6</Color>
    <Color x:Key="StudioGlowColor">#6635C8FF</Color>

    <!-- Brushes -->
    <LinearGradientBrush x:Key="StudioBackgroundBrush" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="#FF050A18" Offset="0" />
        <GradientStop Color="#FF040713" Offset="0.35" />
        <GradientStop Color="#FF01050D" Offset="1" />
    </LinearGradientBrush>
    <LinearGradientBrush x:Key="StudioPanelBrush" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="#FF0F223F" Offset="0" />
        <GradientStop Color="#FF081226" Offset="1" />
    </LinearGradientBrush>
    <LinearGradientBrush x:Key="StudioPanelAltBrush" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#FF152E57" Offset="0" />
        <GradientStop Color="#FF0B1D3B" Offset="1" />
    </LinearGradientBrush>
    <LinearGradientBrush x:Key="StudioPanelHighlightBrush" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#FF1F3D6B" Offset="0" />
        <GradientStop Color="#FF112445" Offset="1" />
    </LinearGradientBrush>
    <SolidColorBrush x:Key="StudioAccentBrush" Color="{StaticResource StudioAccentNeonBlueColor}" />
    <SolidColorBrush x:Key="StudioAccentBrightBrush" Color="{StaticResource StudioAccentNeonBlueBrightColor}" />
    <SolidColorBrush x:Key="StudioAccentUltravioletBrush" Color="{StaticResource StudioAccentUltravioletColor}" />
    <SolidColorBrush x:Key="StudioMutedTextBrush" Color="{StaticResource StudioMutedTextColor}" />
    <SolidColorBrush x:Key="StudioMetadataTextBrush" Color="{StaticResource StudioMetadataTextColor}" />
    <SolidColorBrush x:Key="StudioQueueTextBrush" Color="{StaticResource StudioQueueTextColor}" />
    <SolidColorBrush x:Key="StudioDangerBrush" Color="{StaticResource StudioDangerColor}" />
    <SolidColorBrush x:Key="StudioControlBorderBrush" Color="{StaticResource StudioControlBorderColor}" />
    <SolidColorBrush x:Key="StudioLedBrush" Color="{StaticResource StudioLedColor}" />
    <SolidColorBrush x:Key="StudioPanelBorderBrush" Color="#FF1E2C47" />
    <SolidColorBrush x:Key="StudioInputBackgroundBrush" Color="#FF111A2C" />
    <SolidColorBrush x:Key="StudioInputBorderBrush" Color="#FF2E4A71" />
    <SolidColorBrush x:Key="StudioInputFocusBackgroundBrush" Color="#FF172746" />
    <SolidColorBrush x:Key="StudioMenuBackgroundBrush" Color="#FF060B16" />
    <SolidColorBrush x:Key="StudioMenuHighlightBrush" Color="#FF1C3055" />
    <SolidColorBrush x:Key="StudioMenuHighlightTextBrush" Color="#FFFFFFFF" />
    <DropShadowEffect x:Key="StudioNeonGlowEffect"
                      Color="{StaticResource StudioGlowColor}"
                      BlurRadius="20"
                      ShadowDepth="0"
                      Opacity="0.85" />

    <!-- Global window typography -->
    <Style TargetType="Window">
        <Setter Property="FontFamily" Value="Bahnschrift, Segoe UI" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Background" Value="{StaticResource StudioBackgroundBrush}" />
        <Setter Property="Foreground" Value="{StaticResource StudioMetadataTextBrush}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="UseLayoutRounding" Value="True" />
    </Style>

    <!-- Navigation + menus -->
    <Style TargetType="Menu">
        <Setter Property="Background" Value="{StaticResource StudioMenuBackgroundBrush}" />
        <Setter Property="Foreground" Value="{StaticResource StudioMetadataTextBrush}" />
        <Setter Property="FontFamily" Value="Bahnschrift, Segoe UI" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>

    <Style TargetType="ContextMenu">
        <Setter Property="Background" Value="{StaticResource StudioMenuBackgroundBrush}" />
        <Setter Property="Foreground" Value="{StaticResource StudioMetadataTextBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource StudioAccentBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Effect" Value="{StaticResource StudioNeonGlowEffect}" />
        <Setter Property="Padding" Value="6" />
    </Style>

    <ControlTemplate x:Key="StudioTopLevelMenuItemTemplate" TargetType="MenuItem">
        <Grid SnapsToDevicePixels="True">
            <Border x:Name="Outer"
                    Background="Transparent"
                    BorderBrush="Transparent"
                    BorderThickness="0"
                    CornerRadius="6"
                    Padding="12,6">
                <ContentPresenter x:Name="TopLevelHeader"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"
                                  RecognizesAccessKey="True"
                                  ContentSource="Header" />
            </Border>
            <Popup x:Name="Popup"
                   Placement="Bottom"
                   IsOpen="{TemplateBinding IsSubmenuOpen}"
                   AllowsTransparency="True"
                   Focusable="False"
                   PopupAnimation="Slide">
                <Border Background="{StaticResource StudioMenuBackgroundBrush}"
                        BorderBrush="{StaticResource StudioAccentBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="4"
                        Effect="{StaticResource StudioNeonGlowEffect}">
                    <ScrollViewer CanContentScroll="True"
                                  VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Disabled">
                        <StackPanel IsItemsHost="True"
                                    KeyboardNavigation.DirectionalNavigation="Contained" />
                    </ScrollViewer>
                </Border>
            </Popup>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter TargetName="Outer" Property="Background" Value="{StaticResource StudioMenuHighlightBrush}" />
                <Setter TargetName="Outer" Property="BorderBrush" Value="{StaticResource StudioAccentBrush}" />
                <Setter TargetName="TopLevelHeader" Property="TextElement.Foreground" Value="{StaticResource StudioMenuHighlightTextBrush}" />
            </Trigger>
            <Trigger Property="IsSubmenuOpen" Value="True">
                <Setter TargetName="Outer" Property="Background" Value="{StaticResource StudioMenuHighlightBrush}" />
                <Setter TargetName="Outer" Property="BorderBrush" Value="{StaticResource StudioAccentBrightBrush}" />
                <Setter TargetName="TopLevelHeader" Property="TextElement.Foreground" Value="#FF040A18" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="Outer" Property="Opacity" Value="0.4" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <ControlTemplate x:Key="StudioSubMenuItemTemplate" TargetType="MenuItem">
        <Grid SnapsToDevicePixels="True" Margin="0,1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
                <Border x:Name="Outer"
                    Grid.ColumnSpan="3"
                    Background="Transparent"
                    BorderBrush="Transparent"
                    BorderThickness="1"
                    CornerRadius="4"
                    Padding="10,4" />
                <ContentPresenter x:Name="SubmenuHeader"
                          Grid.Column="1"
                          VerticalAlignment="Center"
                          RecognizesAccessKey="True"
                          ContentSource="Header" />
            <Path x:Name="Glyph"
                  Grid.Column="2"
                  Data="M 0 0 L 6 4 L 0 8 Z"
                  Width="8"
                  Height="8"
                  Fill="{StaticResource StudioMutedTextBrush}"
                  HorizontalAlignment="Center"
                  VerticalAlignment="Center"
                  Visibility="Collapsed" />
            <Popup x:Name="Popup"
                   Placement="Right"
                   HorizontalOffset="4"
                   IsOpen="{TemplateBinding IsSubmenuOpen}"
                   AllowsTransparency="True"
                   Focusable="False"
                   PopupAnimation="Slide">
                <Border Background="{StaticResource StudioMenuBackgroundBrush}"
                        BorderBrush="{StaticResource StudioAccentBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="4"
                        Effect="{StaticResource StudioNeonGlowEffect}">
                    <ScrollViewer CanContentScroll="True"
                                  VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Disabled">
                        <StackPanel IsItemsHost="True"
                                    KeyboardNavigation.DirectionalNavigation="Contained" />
                    </ScrollViewer>
                </Border>
            </Popup>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter TargetName="Outer" Property="Background" Value="{StaticResource StudioMenuHighlightBrush}" />
                <Setter TargetName="Outer" Property="BorderBrush" Value="{StaticResource StudioAccentBrush}" />
                <Setter TargetName="SubmenuHeader" Property="TextElement.Foreground" Value="{StaticResource StudioMenuHighlightTextBrush}" />
                <Setter TargetName="Glyph" Property="Fill" Value="{StaticResource StudioMenuHighlightTextBrush}" />
            </Trigger>
            <Trigger Property="IsSubmenuOpen" Value="True">
                <Setter TargetName="Outer" Property="Background" Value="{StaticResource StudioMenuHighlightBrush}" />
                <Setter TargetName="Outer" Property="BorderBrush" Value="{StaticResource StudioAccentBrightBrush}" />
                <Setter TargetName="SubmenuHeader" Property="TextElement.Foreground" Value="#FF040A18" />
                <Setter TargetName="Glyph" Property="Fill" Value="#FF040A18" />
            </Trigger>
            <Trigger Property="HasItems" Value="True">
                <Setter TargetName="Glyph" Property="Visibility" Value="Visible" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="Outer" Property="Opacity" Value="0.4" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style TargetType="MenuItem">
        <Setter Property="Foreground" Value="{StaticResource StudioMetadataTextBrush}" />
        <Setter Property="FontFamily" Value="Bahnschrift, Segoe UI" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Template" Value="{StaticResource StudioTopLevelMenuItemTemplate}" />
        <Style.Triggers>
            <Trigger Property="Role" Value="TopLevelHeader">
                <Setter Property="Template" Value="{StaticResource StudioTopLevelMenuItemTemplate}" />
            </Trigger>
            <Trigger Property="Role" Value="TopLevelItem">
                <Setter Property="Template" Value="{StaticResource StudioTopLevelMenuItemTemplate}" />
            </Trigger>
            <Trigger Property="Role" Value="SubmenuHeader">
                <Setter Property="Template" Value="{StaticResource StudioSubMenuItemTemplate}" />
            </Trigger>
            <Trigger Property="Role" Value="SubmenuItem">
                <Setter Property="Template" Value="{StaticResource StudioSubMenuItemTemplate}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Text styles -->
    <Style TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource StudioMetadataTextBrush}" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="FontFamily" Value="Bahnschrift, Segoe UI" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="TextOptions.TextFormattingMode" Value="Display" />
        <Setter Property="TextOptions.TextRenderingMode" Value="Auto" />
        <Setter Property="TextOptions.TextHintingMode" Value="Animated" />
    </Style>

    <Style TargetType="Label">
        <Setter Property="Foreground" Value="{StaticResource StudioMetadataTextBrush}" />
        <Setter Property="FontFamily" Value="Bahnschrift SemiBold, Segoe UI" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Margin" Value="0,0,0,4" />
        <Setter Property="TextOptions.TextFormattingMode" Value="Display" />
    </Style>

    <Style x:Key="MetadataLabelTextStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="12" />
        <Setter Property="FontFamily" Value="Bahnschrift, Segoe UI" />
        <Setter Property="Foreground" Value="{StaticResource StudioMutedTextBrush}" />
        <Setter Property="TextOptions.TextFormattingMode" Value="Display" />
        <Setter Property="TextOptions.TextRenderingMode" Value="Auto" />
        <Setter Property="Margin" Value="0,2,8,0" />
    </Style>

    <Style x:Key="MetadataValueTextStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="14" />
        <Setter Property="FontFamily" Value="Bahnschrift, Segoe UI" />
        <Setter Property="Foreground" Value="{StaticResource StudioMetadataTextBrush}" />
        <Setter Property="TextWrapping" Value="NoWrap" />
        <Setter Property="Margin" Value="0,2,0,0" />
    </Style>

    <Style x:Key="QueueItemTextStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="13" />
        <Setter Property="FontFamily" Value="Bahnschrift SemiBold, Segoe UI" />
        <Setter Property="Foreground" Value="{StaticResource StudioQueueTextBrush}" />
        <Setter Property="FontWeight" Value="SemiBold" />
    </Style>

    <!-- Inputs and selectors -->
    <Style TargetType="TextBox">
        <Setter Property="Background" Value="{StaticResource StudioInputBackgroundBrush}" />
        <Setter Property="Foreground" Value="{StaticResource StudioMetadataTextBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource StudioInputBorderBrush}" />
        <Setter Property="BorderThickness" Value="1.2" />
        <Setter Property="Padding" Value="10,7" />
        <Setter Property="Margin" Value="0,0,0,14" />
        <Setter Property="CaretBrush" Value="{StaticResource StudioAccentBrightBrush}" />
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect Color="#99000000" BlurRadius="10" ShadowDepth="0" Opacity="0.55" />
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                <Setter Property="BorderBrush" Value="{StaticResource StudioAccentBrightBrush}" />
                <Setter Property="Background" Value="{StaticResource StudioInputFocusBackgroundBrush}" />
                <Setter Property="Effect" Value="{StaticResource StudioNeonGlowEffect}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.6" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="PasswordBox">
        <Setter Property="Background" Value="{StaticResource StudioInputBackgroundBrush}" />
        <Setter Property="Foreground" Value="{StaticResource StudioMetadataTextBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource StudioInputBorderBrush}" />
        <Setter Property="BorderThickness" Value="1.2" />
        <Setter Property="Padding" Value="10,7" />
        <Setter Property="Margin" Value="0,0,0,14" />
        <Setter Property="CaretBrush" Value="{StaticResource StudioAccentBrightBrush}" />
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect Color="#99000000" BlurRadius="10" ShadowDepth="0" Opacity="0.55" />
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                <Setter Property="BorderBrush" Value="{StaticResource StudioAccentBrightBrush}" />
                <Setter Property="Background" Value="{StaticResource StudioInputFocusBackgroundBrush}" />
                <Setter Property="Effect" Value="{StaticResource StudioNeonGlowEffect}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.6" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="ComboBox">
        <Setter Property="Foreground" Value="{StaticResource StudioMetadataTextBrush}" />
        <Setter Property="Background" Value="{StaticResource StudioInputBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource StudioInputBorderBrush}" />
        <Setter Property="BorderThickness" Value="1.2" />
        <Setter Property="Margin" Value="0,0,0,14" />
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect Color="#66000000" BlurRadius="10" ShadowDepth="0" Opacity="0.65" />
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid x:Name="TemplateRoot" SnapsToDevicePixels="True">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="36" />
                        </Grid.ColumnDefinitions>

                        <Border x:Name="OuterBorder"
                                Grid.ColumnSpan="2"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="4" />

                        <ContentPresenter x:Name="ContentPresenter"
                                          Margin="10,4,10,4"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Left"
                                          RecognizesAccessKey="True"
                                          Content="{TemplateBinding SelectionBoxItem}" />

                        <ToggleButton x:Name="ToggleButton"
                                      Grid.Column="1"
                                      Focusable="False"
                                      Cursor="Hand"
                                      Background="{StaticResource StudioAccentBrush}"
                                      BorderBrush="{StaticResource StudioAccentBrightBrush}"
                                      BorderThickness="0"
                                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                      ClickMode="Press">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="0,6,6,0">
                                        <Grid>
                                            <Path Data="M 0 0 L 10 0 L 5 6 Z"
                                                  Fill="#FF040913"
                                                  Stretch="Uniform"
                                                  Width="12"
                                                  Height="8"
                                                  HorizontalAlignment="Center"
                                                  VerticalAlignment="Center" />
                                        </Grid>
                                    </Border>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>

                        <Popup x:Name="Popup"
                               Placement="Bottom"
                               IsOpen="{TemplateBinding IsDropDownOpen}"
                               AllowsTransparency="True"
                               Focusable="False"
                               PopupAnimation="Slide">
                            <Border x:Name="PopupBorder"
                                    Background="{StaticResource StudioPanelAltBrush}"
                                    BorderBrush="{StaticResource StudioAccentBrush}"
                                    BorderThickness="1"
                                    CornerRadius="6"
                                    Padding="2"
                                    Effect="{StaticResource StudioNeonGlowEffect}">
                                <ScrollViewer Margin="0"
                                              SnapsToDevicePixels="True"
                                              CanContentScroll="True">
                                    <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="HasItems" Value="False">
                            <Setter TargetName="ContentPresenter" Property="MinHeight" Value="28" />
                        </Trigger>
        	            <Trigger Property="IsDropDownOpen" Value="True">
                            <Setter TargetName="OuterBorder" Property="BorderBrush" Value="{StaticResource StudioAccentBrightBrush}" />
                            <Setter TargetName="OuterBorder" Property="Effect" Value="{StaticResource StudioNeonGlowEffect}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ComboBoxItem">
        <Setter Property="Foreground" Value="{StaticResource StudioMetadataTextBrush}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Padding" Value="10,6" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#331F9CFF" />
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="#6625DFFF" />
                <Setter Property="Foreground" Value="{StaticResource StudioMetadataTextBrush}" />
                <Setter Property="FontWeight" Value="Bold" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="CheckBox">
        <Setter Property="Foreground" Value="{StaticResource StudioMetadataTextBrush}" />
        <Setter Property="FontFamily" Value="Bahnschrift, Segoe UI" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Margin" Value="0,0,0,12" />
    </Style>

    <Style TargetType="RadioButton">
        <Setter Property="Foreground" Value="{StaticResource StudioMetadataTextBrush}" />
        <Setter Property="FontFamily" Value="Bahnschrift, Segoe UI" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Margin" Value="0,0,0,12" />
    </Style>

    <Style TargetType="Slider">
        <Setter Property="Foreground" Value="{StaticResource StudioMetadataTextBrush}" />
    </Style>

    <Style TargetType="ListBox">
        <Setter Property="Background" Value="#FF0B1627" />
        <Setter Property="Foreground" Value="{StaticResource StudioMetadataTextBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource StudioControlBorderBrush}" />
        <Setter Property="BorderThickness" Value="1.2" />
        <Setter Property="Padding" Value="6" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect Color="#99000000" BlurRadius="14" ShadowDepth="0" Opacity="0.6" />
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ListBoxItem">
        <Setter Property="Foreground" Value="{StaticResource StudioMetadataTextBrush}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="8,4" />
        <Setter Property="Margin" Value="0,2" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#221E90FF" />
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="#5525DFFF" />
                <Setter Property="BorderBrush" Value="{StaticResource StudioAccentBrightBrush}" />
                <Setter Property="FontWeight" Value="SemiBold" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="ListView">
        <Setter Property="Background" Value="#FF0B1627" />
        <Setter Property="Foreground" Value="{StaticResource StudioMetadataTextBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource StudioControlBorderBrush}" />
        <Setter Property="BorderThickness" Value="1.2" />
        <Setter Property="Padding" Value="4" />
    </Style>

    <Style TargetType="ListViewItem">
        <Setter Property="Padding" Value="8,4" />
        <Setter Property="Margin" Value="0,2" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="1" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#221E90FF" />
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="#5525DFFF" />
                <Setter Property="BorderBrush" Value="{StaticResource StudioAccentBrightBrush}" />
                <Setter Property="FontWeight" Value="SemiBold" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="TabControl">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="{StaticResource StudioControlBorderBrush}" />
        <Setter Property="Foreground" Value="{StaticResource StudioMetadataTextBrush}" />
        <Setter Property="Padding" Value="0" />
    </Style>

    <Style TargetType="TabItem">
        <Setter Property="Foreground" Value="{StaticResource StudioMetadataTextBrush}" />
        <Setter Property="Padding" Value="14,8" />
        <Setter Property="Margin" Value="0,0,6,0" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Grid>
                        <Border x:Name="Border"
                                Background="Transparent"
                                BorderBrush="{StaticResource StudioControlBorderBrush}"
                                BorderThickness="0,0,0,2"
                                CornerRadius="4">
                            <ContentPresenter x:Name="ContentSite"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Center"
                                              ContentSource="Header"
                                              RecognizesAccessKey="True" />
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource StudioAccentBrightBrush}" />
                            <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource StudioAccentBrush}" />
                            <Setter TargetName="Border" Property="BorderThickness" Value="0,0,0,3" />
                            <Setter Property="Foreground" Value="#FF040A18" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="#3325DFFF" />
                            <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource StudioAccentBrightBrush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.4" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="GridViewColumnHeader">
        <Setter Property="Foreground" Value="{StaticResource StudioMetadataTextBrush}" />
        <Setter Property="Background" Value="{StaticResource StudioPanelAltBrush}" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="BorderBrush" Value="{StaticResource StudioAccentUltravioletBrush}" />
        <Setter Property="BorderThickness" Value="0,0,0,2" />
    </Style>

    <!-- Common containers -->
    <Style TargetType="GroupBox">
        <Setter Property="Foreground" Value="{StaticResource StudioMetadataTextBrush}" />
        <Setter Property="FontFamily" Value="Bahnschrift, Segoe UI" />
        <Setter Property="Margin" Value="0,0,0,18" />
        <Setter Property="Padding" Value="12" />
        <Setter Property="BorderBrush" Value="{StaticResource StudioAccentUltravioletBrush}" />
    </Style>

    <Style TargetType="Border" x:Key="PanelBorderStyle">
        <Setter Property="Background" Value="{StaticResource StudioPanelBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource StudioPanelBorderBrush}" />
        <Setter Property="BorderThickness" Value="1.4" />
        <Setter Property="CornerRadius" Value="12" />
        <Setter Property="Padding" Value="18" />
        <Setter Property="Effect" Value="{StaticResource StudioNeonGlowEffect}" />
    </Style>

    <!-- Green LED indicator -->
    <Style x:Key="GreenLedIndicatorStyle" TargetType="Ellipse">
        <Setter Property="Width" Value="14" />
        <Setter Property="Height" Value="14" />
        <Setter Property="Fill" Value="{StaticResource StudioLedBrush}" />
        <Setter Property="Stroke" Value="#FF0D1A12" />
        <Setter Property="StrokeThickness" Value="1" />
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect Color="#FF1FFF9E" BlurRadius="12" ShadowDepth="0" Opacity="0.65" />
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Visibility" Value="Collapsed" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Button styles -->
    <Style TargetType="Button">
        <Setter Property="Foreground" Value="{StaticResource StudioMetadataTextBrush}" />
        <Setter Property="FontFamily" Value="Bahnschrift SemiBold, Segoe UI" />
        <Setter Property="Background">
            <Setter.Value>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#FF204169" Offset="0" />
                    <GradientStop Color="#FF0F1F36" Offset="1" />
                </LinearGradientBrush>
            </Setter.Value>
        </Setter>
        <Setter Property="BorderBrush" Value="{StaticResource StudioAccentUltravioletBrush}" />
        <Setter Property="BorderThickness" Value="1.4" />
        <Setter Property="Padding" Value="12,7" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Margin" Value="6,0,0,0" />
        <Setter Property="Effect" Value="{StaticResource StudioNeonGlowEffect}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background">
                                <Setter.Value>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                        <GradientStop Color="#FF2E66A0" Offset="0" />
                                        <GradientStop Color="#FF12304E" Offset="1" />
                                    </LinearGradientBrush>
                                </Setter.Value>
                            </Setter>
                            <Setter Property="BorderBrush" Value="{StaticResource StudioAccentBrightBrush}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="#FF081223" />
                            <Setter Property="BorderBrush" Value="{StaticResource StudioAccentBrush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.35" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="TransportButtonStyle" TargetType="Button">
        <Setter Property="Foreground" Value="{StaticResource StudioMetadataTextBrush}" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Padding" Value="12,6" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="BorderBrush" Value="{StaticResource StudioAccentBrush}" />
        <Setter Property="Background">
            <Setter.Value>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#FF154067" Offset="0" />
                    <GradientStop Color="#FF0A1626" Offset="1" />
                </LinearGradientBrush>
            </Setter.Value>
        </Setter>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect Color="{StaticResource StudioTransportShadowColor}" BlurRadius="16" ShadowDepth="2" Opacity="0.85" />
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            CornerRadius="4"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#FF24507E" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="#FF0A1624" />
                            <Setter Property="BorderBrush" Value="{StaticResource StudioAccentBrightBrush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.4" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="CartButtonStyle" TargetType="Button">
        <Setter Property="Foreground" Value="{StaticResource StudioMetadataTextBrush}" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Padding" Value="8" />
        <Setter Property="Margin" Value="4" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="BorderBrush" Value="{StaticResource StudioAccentUltravioletBrush}" />
        <Setter Property="Background" Value="#FF111B2E" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            CornerRadius="6"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#FF1F2E4A" />
                            <Setter Property="BorderBrush" Value="{StaticResource StudioAccentBrightBrush}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="#FF070C15" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Misc panel styling helpers -->
    <Style x:Key="StudioPanelBorderStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource StudioPanelBrush}" />
        <Setter Property="CornerRadius" Value="12" />
        <Setter Property="BorderBrush" Value="{StaticResource StudioControlBorderBrush}" />
        <Setter Property="BorderThickness" Value="1.4" />
        <Setter Property="Padding" Value="18" />
        <Setter Property="Effect" Value="{StaticResource StudioNeonGlowEffect}" />
    </Style>

    <Style TargetType="ToggleButton">
        <Setter Property="Foreground" Value="{StaticResource StudioMetadataTextBrush}" />
        <Setter Property="Background" Value="#FF121C2E" />
        <Setter Property="BorderBrush" Value="{StaticResource StudioInputBorderBrush}" />
        <Setter Property="BorderThickness" Value="1.2" />
        <Setter Property="Padding" Value="12,6" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect Color="#99000000" BlurRadius="10" ShadowDepth="0" Opacity="0.55" />
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#FF1A2840" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#FF0E1825" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.45" />
                        </Trigger>
                        <!-- No IsChecked trigger - appearance stays the same, only LED indicator changes -->
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
